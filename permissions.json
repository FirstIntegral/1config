{
  "_comment": [
    "CANONICAL permission policy for ALL THREE tools (Claude Code, Grok, OpenCode).",
    "Edit ONLY here, then run `bash ~/.agents/setup.sh`. Never hand-edit the per-tool copies.",
    "setup.sh replaces generated permission buckets from this file so revocations propagate; other config survives:",
    "  [5b] Claude   -> ~/.claude/settings.json            permissions.allow / permissions.ask / permissions.deny",
    "  [5c] Grok     -> ~/.grok/config.toml [permission]   allow / ask / deny                    (verbatim rules)",
    "  [5d] OpenCode -> ~/.config/opencode/opencode.jsonc  permission.bash map                   (Bash(X) -> \"X\": allow|ask|deny)",
    "Rule syntax below is Claude-native. Grok/OpenCode entries are derived from enforceable Bash(...) rules;",
    "non-Bash rules (Skill(...) etc.) are Claude-only. OpenCode gets a managed catch-all: allow when",
    "bash_without_prompt=true, otherwise ask. Allow then (if autonomy off) ask then deny follow it.",
    "OpenCode last-match: a later git push ask beats *:allow. Grok always-approve still honors",
    "shell ask. So while bash_without_prompt is true, setup OMITS ask fan-out for all three tools.",
    "Allow entries run WITHOUT a prompt; ask entries prompt; deny entries block. Unmatched Bash asks by default.",
    "Deliberately absent from allow: rm, sudo, curl/wget, git push, package installs, chmod/chown, dd, mv.",
    "Also absent on purpose: xargs -I / sh -c / bash -c / npx — they run an arbitrary inner command, so",
    "allowing them would launder every rule here. 'sed *' IS allowed and covers in-place 'sed -i' writes;",
    "that is intended (edits are prompt-free anyway, see defaults.edit_without_prompt).",
    "Exception: `bash ~/.agents/sync.sh` is allowlisted. It is the narrow verified push path, and it",
    "can only push signed main commits from the ~/.agents repo root after strict PASS with zero warnings.",
    "Generic `git push` stays in the canonical ask list (restore-gate). Live tools omit that ask while",
    "bash_without_prompt is true — otherwise OpenCode last-match and Grok shell-ask re-prompt it.",
    "Deny `rm -rf` is exact `/`, `~`, `$HOME` only. Do NOT add `rm -rf /*` / `~/*` / `$HOME/*`:",
    "all three matchers treat `*` as a glob, so those rules deny every `rm -rf /…` and `rm -rf ~/…`",
    "(OpenCode last-match, 2026-09-05: Vigil Y-allow then harness still blocked wiping ~/.config/protonmail).",
    "Literal `rm -rf /*` / `~/*` / `$HOME/*` is Vigil rm-root (classifier), not a harness glob.",
    "Matching notes: wrappers timeout/time/nice/nohup/stdbuf/command/builtin/bare-xargs are stripped before",
    "matching, so 'Bash(pytest *)' already covers 'timeout 900 pytest -q'. Interpreter PATHS are not",
    "normalized, hence the explicit .venv/venv entries below. Heredocs whose body mixes braces with quotes",
    "(Python f-strings) are flagged 'expansion obfuscation' and prompt regardless of any rule here — write",
    "the script to a scratchpad file and run the file instead (see AGENTS.md).",
    "COMPOUND COMMANDS: a pipeline/;/&&-chain is approved only if EVERY segment matches a rule. One stray",
    "'lscpu' in a ten-part probe prompts for the whole line — hence the bare forms next to the '* ' forms."
  ],
  "defaults": {
    "_comment": [
      "Tool-agnostic autonomy defaults. setup.sh translates each key per tool, verify.sh checks all three.",
      "edit_without_prompt: writing/creating files needs no prompt.",
      "  claude   -> used only when bash_without_prompt is false: defaultMode = \"acceptEdits\"",
      "  grok     -> [ui] permission_mode = \"acceptEdits\"",
      "  opencode -> permission.edit = \"allow\"",
      "bash_without_prompt: NO bash permission prompts. Required for Claude — allowlist alone cannot",
      "kill hard-coded safety prompts (cd+write path-bypass, cd+git untrusted-hooks, parse verdicts).",
      "  claude   -> permissions.defaultMode = \"bypassPermissions\" (takes precedence over acceptEdits)",
      "  grok     -> [ui] permission_mode = \"always-approve\"",
      "  opencode -> permission.bash catch-all \"*\" = \"allow\"; ask patterns omitted (last-match)",
      "  opencode also gets permission.external_directory = allow while bash_without_prompt is true",
      "  (OpenCode-only gate: file access outside project cwd, e.g. /tmp scratch scripts — Claude",
      "  bypass and Grok always-approve cover that natively). Otherwise external_directory = ask.",
      "With bash_without_prompt true, setup omits ask fan-out (OpenCode last-match and Grok",
      "shell-ask would otherwise still prompt git push). Deny still copies. Claude deny is",
      "best-effort only — bypass skips checks. Current policy (user chose full autonomy",
      "2026-08-29): edit_without_prompt=true AND bash_without_prompt=true — no Bash prompts",
      "in any tool; generic git push no longer asks. Flip back to false to restore the gate."
    ],
    "edit_without_prompt": true,
    "bash_without_prompt": true
  },
  "permissions": {
    "allow": [
      "Bash(python3 *)",
      "Bash(python *)",
      "Bash(pytest *)",
      "Bash(.venv/bin/python *)",
      "Bash(.venv/bin/python3 *)",
      "Bash(.venv/bin/pytest *)",
      "Bash(./.venv/bin/python *)",
      "Bash(venv/bin/python *)",
      "Bash(venv/bin/pytest *)",
      "Bash(uv run *)",
      "Bash(source .venv/bin/activate *)",
      "Bash(grep *)",
      "Bash(rg *)",
      "Bash(ls *)",
      "Bash(cat *)",
      "Bash(head *)",
      "Bash(tail *)",
      "Bash(wc *)",
      "Bash(diff *)",
      "Bash(seq *)",
      "Bash(sort *)",
      "Bash(uniq *)",
      "Bash(cut *)",
      "Bash(tr *)",
      "Bash(jq *)",
      "Bash(find *)",
      "Bash(stat *)",
      "Bash(du *)",
      "Bash(realpath *)",
      "Bash(basename *)",
      "Bash(dirname *)",
      "Bash(which *)",
      "Bash(command -v *)",
      "Bash(printf *)",
      "Bash(echo *)",
      "Bash(env)",
      "Bash(sed *)",
      "Bash(awk *)",
      "Bash(nvidia-smi *)",
      "Bash(nvidia-smi)",
      "Bash(uname *)",
      "Bash(uname)",
      "Bash(lscpu *)",
      "Bash(lscpu)",
      "Bash(lsb_release *)",
      "Bash(hostnamectl)",
      "Bash(nproc *)",
      "Bash(nproc)",
      "Bash(free *)",
      "Bash(free)",
      "Bash(df *)",
      "Bash(df)",
      "Bash(lsblk *)",
      "Bash(lsblk)",
      "Bash(lspci *)",
      "Bash(lspci)",
      "Bash(vmstat *)",
      "Bash(getconf *)",
      "Bash(uptime)",
      "Bash(id)",
      "Bash(whoami)",
      "Bash(pwd)",
      "Bash(date *)",
      "Bash(date)",
      "Bash(ps *)",
      "Bash(pgrep *)",
      "Bash(file *)",
      "Bash(readlink *)",
      "Bash(tree *)",
      "Bash(sha256sum *)",
      "Bash(md5sum *)",
      "Bash(crontab -l)",
      "Bash(cp *)",
      "Bash(mkdir -p *)",
      "Bash(git status *)",
      "Bash(git log *)",
      "Bash(git diff *)",
      "Bash(git show *)",
      "Bash(git branch)",
      "Bash(git remote -v)",
      "Bash(git remote get-url *)",
      "Bash(git ls-remote *)",
      "Bash(git rev-parse *)",
      "Bash(git ls-files *)",
      "Bash(git config --get *)",
      "Bash(git describe *)",
      "Bash(git blame *)",
      "Bash(tectonic *)",
      "Bash(latexmk *)",
      "Bash(pdflatex *)",
      "Bash(xelatex *)",
      "Bash(lualatex *)",
      "Bash(bibtex *)",
      "Bash(biber *)",
      "Bash(makeindex *)",
      "Bash(kpsewhich *)",
      "Bash(texcount *)",
      "Bash(latexdiff *)",
      "Bash(pdfinfo *)",
      "Bash(pdftotext *)",
      "Bash(pdftoppm *)",
      "Bash(bash docs/paper/build.sh)",
      "Bash(bash docs/paper/build.sh *)",
      "Bash(bash ~/.agents/setup.sh)",
      "Bash(bash ~/.agents/verify.sh)",
      "Bash(bash ~/.agents/sync.sh)",
      "Bash(bash ~/.agents/sync.sh *)",
      "Bash(bash ~/.agents/hooks/brain-sync.sh)"
    ],
    "ask": [
      "Bash(git push)",
      "Bash(git push *)"
    ],
    "deny": [
      "Bash(rm -rf /)",
      "Bash(rm -rf ~)",
      "Bash(rm -rf $HOME)",
      "Bash(sudo rm *)",
      "Bash(mkfs*)",
      "Bash(dd if=* of=/dev/*)"
    ]
  }
}
